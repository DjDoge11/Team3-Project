# TODO

## Theme redesign (white + vibrant teal)

- [ ] Inspect and unify theme tokens across global/design-system CSS
- [ ] Update `src/index.css` defaults (body bg, text colors, link styles)
- [ ] Tune `src/styles/layout.css` navbar/footer gradients + shadows for teal/white consistency
- [ ] Tune `src/styles/components.css` buttons/inputs/cards/modals to use design tokens only
- [ ] Tune `src/styles/animations.css` teal glow/hover effects if needed
- [ ] Migrate legacy overrides in `src/App.css`
- [ ] Migrate page-level legacy styles in `src/pages/*.css`
- [x] Run `npm run build` (and/or `npm run lint`) to ensure no regressions

- [x] Spot-check key pages: Home, About, Services, Courses, Grade, Calculator, Auth, Contact


